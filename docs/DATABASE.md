# 数据库

迁移 `0001_initial` 建立全部业务表：公司、招聘人员、平台账号、岗位/别名/待映射岗位、候选人来源、跟进、面试、不可变事件、冲突/排除、Outbox/通知、设备/设备码、设置、诊断、审计和 Mock 飞书消息。

关键唯一约束包括来源键、事件幂等键、冲突键、Outbox 幂等键、排序后的排除来源对，以及公司/平台/岗位别名。查询索引覆盖姓名+岗位、平台 ID、来源键、阶段、冲突键和到期 Outbox。

升级：`make migrate`。新迁移：`make migration`。迁移不得由应用启动隐式执行。

