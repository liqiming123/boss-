# 安全

系统执行数据最小化：不保存招聘平台密码、Cookie、完整 HTML、聊天、简历或不必要的评价。URL 只保存 SHA-256；诊断字段有白名单。Access/Refresh Token 分离，Refresh Token 在设备表只保存哈希，设备可撤销。写操作受 RBAC 和审计约束，公司级拒绝仅允许管理员/HR 负责人。

生产要求 HTTPS、严格 CORS、强密钥、数据库最小权限、日志脱敏、依赖扫描、备份加密和定期权限审计。插件消息路由使用显式白名单，不从远程加载可执行代码。

