# 运维

- 备份：每日 `pg_dump --format=custom`，加密存储并定期恢复验证。
- Worker：监控 PENDING 积压、FAILED 数量和处理延迟；后台可重试或取消。
- 岗位：待映射岗位由管理员映射，禁止系统擅自创建内部岗位。
- 插件：根据诊断错误码排查，不要求用户上传完整页面。
- 页面改版：冻结对应 adapter、采集脱敏诊断、更新 fixture 和版本后再发布。
- 数据：历史事件和审计不因容量直接删除；按合规策略归档。

