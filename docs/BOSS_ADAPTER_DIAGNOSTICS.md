# BOSS Adapter 诊断

当前 `boss-selectors.ts` 明确为空，不包含猜测选择器。现场适配步骤：

1. 在合法登录且获得公司授权的页面打开扩展诊断。
2. 仅记录页面类型、字段存在状态、候选人/岗位/账号节点的脱敏结构特征。
3. 禁止导出完整 HTML、Cookie、Token、聊天、手机号、简历或页面截图中的个人敏感信息。
4. 在本地匿名 fixture 验证主选择器、有限备用选择器、SPA 变化和字段缺失。
5. 经人工验收后再配置 selector，并更新 adapter 版本和回归测试。

