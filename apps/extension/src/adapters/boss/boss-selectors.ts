export type BossSelectorConfig={account:string[];candidate:string[];job:string[];platformId:string[]};
// Deliberately empty until selectors are verified from a sanitized on-site diagnostic.
export const bossSelectors:BossSelectorConfig={account:[],candidate:[],job:[],platformId:[]};

